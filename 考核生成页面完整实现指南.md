# 考核生成页面集成实验指导书和大作业功能 - 完整实现指南

## 📋 已完成的修改

### 1. 前端模板修改（已完成）
- ✅ 考核类型选项已修改为：考试、实验指导书、大作业
- ✅ 右侧配置区域已根据类型动态显示
- ✅ 删除了绿色的"生成实验指导书"按钮
- ✅ 下一步按钮文本动态显示

---

## 🔧 需要手动添加的代码

### 步骤1：在 ExamGeneration.vue 的 `<script>` 部分添加 import

找到文件中的 import 区域，添加以下代码：

```javascript
import * as smartTeachingApi from '@/api/smartTeaching'
```

### 步骤2：添加配置对象

在 `<script setup>` 或 data() 中添加以下代码：

```javascript
// 实验指导书配置
const experimentConfig = reactive({
  chapterName: '',
  knowledgePoints: [],
  difficultyLevel: 'intermediate'
})

// 大作业配置
const assignmentConfig = reactive({
  title: '',
  description: '',
  submitTypes: ['file'],
  totalScore: 100
})
```

### 步骤3：修改 examConfig 的默认类型

找到 examConfig 的定义，将：
```javascript
type: 'choice'
```
改为：
```javascript
type: 'exam'
```

### 步骤4：添加类型切换方法

```javascript
const onTypeChange = (type) => {
  console.log('考核类型切换为:', type)
  // 重置配置
  if (type === 'experiment') {
    experimentConfig.chapterName = ''
    experimentConfig.knowledgePoints = []
    experimentConfig.difficultyLevel = 'intermediate'
  } else if (type === 'assignment') {
    assignmentConfig.title = ''
    assignmentConfig.description = ''
    assignmentConfig.submitTypes = ['file']
    assignmentConfig.totalScore = 100
  }
}
```

### 步骤5：修改 goToStep 方法

找到现有的 goToStep 方法，修改为：

```javascript
const goToStep = async (step) => {
  if (step === 2) {
    // 如果是实验指导书类型，直接生成
    if (examConfig.type === 'experiment') {
      await generateExperimentGuide()
      return
    }
    // 如果是大作业类型，直接生成
    if (examConfig.type === 'assignment') {
      await generateAssignment()
      return
    }
    // 如果是考试类型，进入下一步配置题目
    wizardStep.value = step
  } else {
    wizardStep.value = step
  }
}
```

### 步骤6：添加生成实验指导书方法

```javascript
const generateExperimentGuide = async () => {
  // 验证必填项
  if (!selectedCourseId.value) {
    ElMessage.warning('请选择课程')
    return
  }
  if (!experimentConfig.chapterName) {
    ElMessage.warning('请输入章节名称')
    return
  }
  if (experimentConfig.knowledgePoints.length === 0) {
    ElMessage.warning('请选择至少一个知识点')
    return
  }

  try {
    generating.value = true
    const teacherId = localStorage.getItem('userId') || '2'
    
    const res = await smartTeachingApi.generateExperimentGuide({
      teacherId: parseInt(teacherId),
      courseId: selectedCourseId.value,
      chapterName: experimentConfig.chapterName,
      knowledgePoints: experimentConfig.knowledgePoints,
      difficultyLevel: experimentConfig.difficultyLevel
    })

    if (res.code === 1 || res.success === true) {
      ElMessage.success('实验指导书生成中，请稍候...')
      
      // 轮询检查状态
      const guideId = res.data
      checkExperimentStatus(guideId)
    } else {
      ElMessage.error('生成失败：' + (res.msg || '未知错误'))
    }
  } catch (error) {
    console.error('生成失败', error)
    ElMessage.error('生成失败：' + (error.message || '未知错误'))
  } finally {
    generating.value = false
  }
}
```

### 步骤7：添加检查实验指导书生成状态方法

```javascript
const checkExperimentStatus = async (guideId) => {
  let attempts = 0
  const maxAttempts = 20
  
  const check = async () => {
    try {
      const res = await smartTeachingApi.getExperimentGuide(guideId)
      if (res.code === 1 && res.data.status === 'completed') {
        ElMessage.success('实验指导书生成完成！')
        // 跳转到历史记录
        goToHistoryView()
        return
      }
      
      attempts++
      if (attempts < maxAttempts) {
        setTimeout(check, 3000)
      } else {
        ElMessage.warning('生成超时，请稍后在历史记录中查看')
        goToHistoryView()
      }
    } catch (error) {
      console.error('检查状态失败', error)
    }
  }
  
  setTimeout(check, 3000)
}
```

### 步骤8：添加生成大作业方法

```javascript
const generateAssignment = async () => {
  // 验证必填项
  if (!selectedCourseId.value) {
    ElMessage.warning('请选择课程')
    return
  }
  if (!assignmentConfig.title) {
    ElMessage.warning('请输入作业标题')
    return
  }
  if (!assignmentConfig.description) {
    ElMessage.warning('请输入作业描述')
    return
  }

  try {
    generating.value = true
    const teacherId = localStorage.getItem('userId') || '2'
    
    // 调用大作业生成API
    const res = await smartTeachingApi.submitHomework({
      teacherId: parseInt(teacherId),
      studentId: parseInt(teacherId), // 临时使用teacherId
      courseId: selectedCourseId.value,
      homeworkTitle: assignmentConfig.title,
      fileUrl: '',
      fileName: '',
      content: assignmentConfig.description
    })

    if (res.code === 1 || res.success === true) {
      ElMessage.success('大作业创建成功！')
      // 跳转到历史记录
      goToHistoryView()
    } else {
      ElMessage.error('创建失败：' + (res.msg || '未知错误'))
    }
  } catch (error) {
    console.error('创建失败', error)
    ElMessage.error('创建失败：' + (error.message || '未知错误'))
  } finally {
    generating.value = false
  }
}
```

### 步骤9：在 return 或 export 中添加变量和方法

如果使用 `<script setup>`，变量会自动暴露。

如果使用 Options API，需要在 return 中添加：

```javascript
return {
  experimentConfig,
  assignmentConfig,
  onTypeChange,
  generateExperimentGuide,
  generateAssignment,
  checkExperimentStatus,
  // ... 其他已有的变量和方法
}
```

---

## 🎯 后端API说明

### 实验指导书API（已实现）

后端已经实现了完整的实验指导书生成功能：

- **POST** `/api/smart-teaching/generate-experiment-guide` - 生成实验指导书
- **GET** `/api/smart-teaching/experiment-guide/{guideId}` - 获取详情
- **GET** `/api/smart-teaching/experiment-guides/{teacherId}` - 获取列表
- **DELETE** `/api/smart-teaching/experiment-guide/{guideId}` - 删除记录

### 大作业API（复用作业批阅功能）

大作业功能可以复用现有的作业批阅API：

- **POST** `/api/smart-teaching/submit-homework` - 创建大作业
- **GET** `/api/smart-teaching/homework-list/{teacherId}` - 获取列表
- **DELETE** `/api/smart-teaching/homework/{homeworkId}` - 删除记录

---

## 📝 完整代码示例（script部分）

```javascript
<script setup>
import { ref, reactive, computed, onMounted } from 'vue'
import { ElMessage } from 'element-plus'
import * as smartTeachingApi from '@/api/smartTeaching'
// ... 其他已有的 import

// ... 其他已有的变量定义

// 实验指导书配置
const experimentConfig = reactive({
  chapterName: '',
  knowledgePoints: [],
  difficultyLevel: 'intermediate'
})

// 大作业配置
const assignmentConfig = reactive({
  title: '',
  description: '',
  submitTypes: ['file'],
  totalScore: 100
})

// 修改 examConfig 的 type 默认值为 'exam'
const examConfig = reactive({
  name: '',
  type: 'exam', // 改为 exam
  duration: 60,
  // ... 其他字段
})

// 类型切换方法
const onTypeChange = (type) => {
  console.log('考核类型切换为:', type)
  if (type === 'experiment') {
    experimentConfig.chapterName = ''
    experimentConfig.knowledgePoints = []
    experimentConfig.difficultyLevel = 'intermediate'
  } else if (type === 'assignment') {
    assignmentConfig.title = ''
    assignmentConfig.description = ''
    assignmentConfig.submitTypes = ['file']
    assignmentConfig.totalScore = 100
  }
}

// 修改 goToStep 方法
const goToStep = async (step) => {
  if (step === 2) {
    if (examConfig.type === 'experiment') {
      await generateExperimentGuide()
      return
    }
    if (examConfig.type === 'assignment') {
      await generateAssignment()
      return
    }
    wizardStep.value = step
  } else {
    wizardStep.value = step
  }
}

// 生成实验指导书
const generateExperimentGuide = async () => {
  if (!selectedCourseId.value) {
    ElMessage.warning('请选择课程')
    return
  }
  if (!experimentConfig.chapterName) {
    ElMessage.warning('请输入章节名称')
    return
  }
  if (experimentConfig.knowledgePoints.length === 0) {
    ElMessage.warning('请选择至少一个知识点')
    return
  }

  try {
    generating.value = true
    const teacherId = localStorage.getItem('userId') || '2'
    
    const res = await smartTeachingApi.generateExperimentGuide({
      teacherId: parseInt(teacherId),
      courseId: selectedCourseId.value,
      chapterName: experimentConfig.chapterName,
      knowledgePoints: experimentConfig.knowledgePoints,
      difficultyLevel: experimentConfig.difficultyLevel
    })

    if (res.code === 1 || res.success === true) {
      ElMessage.success('实验指导书生成中，请稍候...')
      const guideId = res.data
      checkExperimentStatus(guideId)
    } else {
      ElMessage.error('生成失败：' + (res.msg || '未知错误'))
    }
  } catch (error) {
    console.error('生成失败', error)
    ElMessage.error('生成失败：' + (error.message || '未知错误'))
  } finally {
    generating.value = false
  }
}

// 检查实验指导书生成状态
const checkExperimentStatus = async (guideId) => {
  let attempts = 0
  const maxAttempts = 20
  
  const check = async () => {
    try {
      const res = await smartTeachingApi.getExperimentGuide(guideId)
      if (res.code === 1 && res.data.status === 'completed') {
        ElMessage.success('实验指导书生成完成！')
        goToHistoryView()
        return
      }
      
      attempts++
      if (attempts < maxAttempts) {
        setTimeout(check, 3000)
      } else {
        ElMessage.warning('生成超时，请稍后在历史记录中查看')
        goToHistoryView()
      }
    } catch (error) {
      console.error('检查状态失败', error)
    }
  }
  
  setTimeout(check, 3000)
}

// 生成大作业
const generateAssignment = async () => {
  if (!selectedCourseId.value) {
    ElMessage.warning('请选择课程')
    return
  }
  if (!assignmentConfig.title) {
    ElMessage.warning('请输入作业标题')
    return
  }
  if (!assignmentConfig.description) {
    ElMessage.warning('请输入作业描述')
    return
  }

  try {
    generating.value = true
    const teacherId = localStorage.getItem('userId') || '2'
    
    const res = await smartTeachingApi.submitHomework({
      teacherId: parseInt(teacherId),
      studentId: parseInt(teacherId),
      courseId: selectedCourseId.value,
      homeworkTitle: assignmentConfig.title,
      fileUrl: '',
      fileName: '',
      content: assignmentConfig.description
    })

    if (res.code === 1 || res.success === true) {
      ElMessage.success('大作业创建成功！')
      goToHistoryView()
    } else {
      ElMessage.error('创建失败：' + (res.msg || '未知错误'))
    }
  } catch (error) {
    console.error('创建失败', error)
    ElMessage.error('创建失败：' + (error.message || '未知错误'))
  } finally {
    generating.value = false
  }
}

// ... 其他已有的方法
</script>
```

---

## ✅ 测试清单

完成上述修改后，请测试以下功能：

1. [ ] 选择"考试"类型，右侧显示题目配置
2. [ ] 选择"实验指导书"类型，右侧显示实验配置表单
3. [ ] 选择"大作业"类型，右侧显示作业配置表单
4. [ ] 填写实验指导书信息，点击"生成实验指导书"按钮，能正常生成
5. [ ] 填写大作业信息，点击"生成大作业"按钮，能正常创建
6. [ ] 生成后能跳转到历史记录查看

---

## 🔍 调试提示

如果遇到问题，请检查：

1. **按钮无反应**：检查浏览器控制台是否有错误
2. **方法未定义**：确认所有方法都已添加到 return 或正确使用 setup
3. **API调用失败**：检查后端服务是否正常运行
4. **变量未定义**：确认 experimentConfig 和 assignmentConfig 已定义

---

## 📞 需要帮助？

如果在实现过程中遇到问题，请提供：
1. 浏览器控制台的错误信息
2. 具体哪一步出现问题
3. 当前的代码结构（是使用 setup 还是 Options API）

