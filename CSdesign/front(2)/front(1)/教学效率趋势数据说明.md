# 教学效率趋势数据来源与计算逻辑说明

## 📊 当前数据状态

### ⚠️ 重要说明
**当前教学效率趋势图表使用的是模拟数据（硬编码），并非从数据库实时获取。**

## 🔍 数据详细说明

### 1. 模拟数据定义位置

文件：`src/components/EfficiencyTrendChart.vue`

```javascript
// 月度模拟数据
const monthlyData = [
  { month: '1月', efficiency: 52, courseCount: 18, studentEngagement: 68 },
  { month: '2月', efficiency: 54, courseCount: 20, studentEngagement: 70 },
  { month: '3月', efficiency: 53, courseCount: 19, studentEngagement: 69 },
  { month: '4月', efficiency: 56, courseCount: 22, studentEngagement: 73 },
  { month: '5月', efficiency: 55, courseCount: 21, studentEngagement: 72 },
  { month: '6月', efficiency: 57.33, courseCount: 24, studentEngagement: 75 },
]

// 周度模拟数据
const weeklyData = [
  { day: '周一', efficiency: 55, hours: 4 },
  { day: '周二', efficiency: 58, hours: 5 },
  { day: '周三', efficiency: 56, hours: 4.5 },
  { day: '周四', efficiency: 59, hours: 5.5 },
  { day: '周五', efficiency: 57, hours: 5 },
  { day: '周六', efficiency: 54, hours: 3 },
  { day: '周日', efficiency: 52, hours: 2 },
]
```

### 2. 各指标的含义

#### 📈 教学效率 (efficiency)
- **定义**：综合评估教师教学效果的百分比指标
- **当前值**：57.33%（月度）/ 57%（周度）
- **模拟数据**：手动设置的固定值

#### 📚 课程数量 (courseCount)
- **定义**：教师在该时间段内教授的课程数量
- **当前值**：24节（6月）
- **模拟数据**：手动设置的固定值

#### 👥 学生参与度 (studentEngagement)
- **定义**：学生在课程中的活跃参与程度百分比
- **当前值**：75%
- **模拟数据**：手动设置的固定值

#### ⏰ 平均课时 (avgCourseHours)
- **定义**：教师在该时间段内的平均授课时长
- **当前值**：21节/月 或 4.3节/天
- **模拟数据**：手动设置的固定值

## 🎯 真实数据应该如何计算

### 方案一：基于现有数据库数据计算

根据后端代码分析，系统已有以下真实数据源：

#### 1️⃣ **教学效率计算公式**

```javascript
教学效率 = (
  平均成绩权重 * 平均成绩百分比 +
  课程完成率权重 * 课程完成率 +
  学生参与度权重 * 学生参与度 +
  考试通过率权重 * 考试通过率
) / 总权重
```

**数据来源：**
- `平均成绩`：从 `student_exam` 表查询教师所有课程的平均分
- `课程完成率`：从 `student_progress` 表计算学生完成课程的比例
- `学生参与度`：从 `student_progress` 表统计学生活跃度
- `考试通过率`：从 `student_exam` 表计算及格率

**后端已有接口：**
```java
// StatsServiceImpl.java
@Override
public TeacherStatsDTO getTeacherStats(Long teacherId) {
    // 获取平均分
    Double avgScore = studentExamMapper.selectAverageScoreByTeacher(teacherId);
    
    // 获取学生总数
    Integer studentCount = courseMapper.countStudentsByTeacherId(teacherId);
    
    // 获取课程数量
    Integer courseCount = courseMapper.countByTeacherId(teacherId);
    
    // 获取教学时长
    Integer totalStudyMinutes = studentProgressMapper.selectTotalStudyTimeByTeacher(teacherId);
    
    // ... 其他统计
}
```

#### 2️⃣ **课程数量**

**数据来源：**
```sql
SELECT COUNT(*) FROM course WHERE teacher_id = ?
```

**后端接口：**
```java
Integer courseCount = courseMapper.countByTeacherId(teacherId);
```

#### 3️⃣ **学生参与度**

**计算方式：**
```javascript
学生参与度 = (活跃学生数 / 总学生数) * 100%
```

**数据来源：**
- 从 `student_progress` 表统计最近一段时间有学习记录的学生
- 从 `course_student` 表获取选课学生总数

#### 4️⃣ **教学时长**

**数据来源：**
```sql
SELECT SUM(study_time) / 60 as teaching_hours
FROM student_progress sp
JOIN course c ON sp.course_id = c.id
WHERE c.teacher_id = ?
```

**后端接口：**
```java
Integer totalStudyMinutes = studentProgressMapper.selectTotalStudyTimeByTeacher(teacherId);
stats.setTeachingHours(totalStudyMinutes / 60);
```

### 方案二：新增教学效率趋势表

为了更准确地追踪教学效率趋势，建议创建专门的统计表：

#### 数据库表设计

```sql
CREATE TABLE teacher_efficiency_trend (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    teacher_id BIGINT NOT NULL COMMENT '教师ID',
    date DATE NOT NULL COMMENT '统计日期',
    period_type VARCHAR(10) NOT NULL COMMENT '周期类型：day/week/month',
    
    -- 核心指标
    efficiency_score DECIMAL(5,2) COMMENT '教学效率分数',
    avg_score DECIMAL(5,2) COMMENT '平均成绩',
    course_count INT COMMENT '课程数量',
    student_count INT COMMENT '学生数量',
    teaching_hours DECIMAL(5,1) COMMENT '教学时长（小时）',
    
    -- 参与度指标
    student_engagement DECIMAL(5,2) COMMENT '学生参与度',
    active_student_count INT COMMENT '活跃学生数',
    completion_rate DECIMAL(5,2) COMMENT '课程完成率',
    
    -- 考试指标
    exam_count INT COMMENT '考试数量',
    pass_rate DECIMAL(5,2) COMMENT '及格率',
    excellent_rate DECIMAL(5,2) COMMENT '优秀率',
    
    -- 时间戳
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    INDEX idx_teacher_date (teacher_id, date),
    INDEX idx_period (period_type, date)
) COMMENT='教师教学效率趋势统计表';
```

#### 定时任务计算

```java
@Scheduled(cron = "0 0 2 * * ?") // 每天凌晨2点执行
public void calculateDailyEfficiency() {
    List<Long> teacherIds = teacherMapper.selectAllTeacherIds();
    
    for (Long teacherId : teacherIds) {
        // 计算昨天的教学效率
        TeacherEfficiencyTrend trend = new TeacherEfficiencyTrend();
        trend.setTeacherId(teacherId);
        trend.setDate(LocalDate.now().minusDays(1));
        trend.setPeriodType("day");
        
        // 计算各项指标
        Double avgScore = calculateAvgScore(teacherId, yesterday);
        Integer courseCount = calculateCourseCount(teacherId, yesterday);
        Double studentEngagement = calculateEngagement(teacherId, yesterday);
        
        // 计算综合效率分数
        Double efficiency = calculateEfficiency(avgScore, courseCount, studentEngagement);
        trend.setEfficiencyScore(efficiency);
        
        // 保存到数据库
        efficiencyTrendMapper.insert(trend);
    }
}
```

## 🔧 如何连接真实数据

### 步骤 1：创建后端API接口

在 `StatsController.java` 中添加：

```java
// 获取教学效率趋势
@GetMapping("/teacher/{teacherId}/efficiency-trend")
public Result getEfficiencyTrend(
    @PathVariable Long teacherId,
    @RequestParam(defaultValue = "month") String type
) {
    Map<String, Object> trend = statsService.getEfficiencyTrend(teacherId, type);
    return Result.success("获取教学效率趋势成功", trend);
}
```

### 步骤 2：实现Service层

在 `StatsServiceImpl.java` 中添加：

```java
@Override
public Map<String, Object> getEfficiencyTrend(Long teacherId, String type) {
    Map<String, Object> result = new HashMap<>();
    
    if ("month".equals(type)) {
        // 获取最近6个月的数据
        List<Map<String, Object>> monthlyData = new ArrayList<>();
        LocalDate now = LocalDate.now();
        
        for (int i = 5; i >= 0; i--) {
            LocalDate month = now.minusMonths(i);
            Map<String, Object> data = new HashMap<>();
            
            // 查询该月的统计数据
            Double efficiency = calculateMonthlyEfficiency(teacherId, month);
            Integer courseCount = courseMapper.countByTeacherAndMonth(teacherId, month);
            Double engagement = calculateMonthlyEngagement(teacherId, month);
            
            data.put("month", month.getMonthValue() + "月");
            data.put("efficiency", efficiency);
            data.put("courseCount", courseCount);
            data.put("studentEngagement", engagement);
            
            monthlyData.add(data);
        }
        
        result.put("monthly", monthlyData);
    } else if ("week".equals(type)) {
        // 获取最近7天的数据
        List<Map<String, Object>> weeklyData = new ArrayList<>();
        LocalDate now = LocalDate.now();
        String[] weekDays = {"周一", "周二", "周三", "周四", "周五", "周六", "周日"};
        
        for (int i = 6; i >= 0; i--) {
            LocalDate day = now.minusDays(i);
            Map<String, Object> data = new HashMap<>();
            
            // 查询该天的统计数据
            Double efficiency = calculateDailyEfficiency(teacherId, day);
            Double hours = calculateDailyHours(teacherId, day);
            
            data.put("day", weekDays[day.getDayOfWeek().getValue() - 1]);
            data.put("efficiency", efficiency);
            data.put("hours", hours);
            
            weeklyData.add(data);
        }
        
        result.put("weekly", weeklyData);
    }
    
    return result;
}

// 计算月度教学效率
private Double calculateMonthlyEfficiency(Long teacherId, LocalDate month) {
    // 获取该月的平均成绩
    Double avgScore = studentExamMapper.selectAvgScoreByTeacherAndMonth(teacherId, month);
    
    // 获取该月的课程完成率
    Double completionRate = studentProgressMapper.selectCompletionRateByMonth(teacherId, month);
    
    // 获取该月的学生参与度
    Double engagement = studentProgressMapper.selectEngagementByMonth(teacherId, month);
    
    // 综合计算效率（可以根据实际需求调整权重）
    Double efficiency = (avgScore * 0.4 + completionRate * 0.3 + engagement * 0.3);
    
    return Math.round(efficiency * 100) / 100.0;
}
```

### 步骤 3：更新前端组件

在 `EfficiencyTrendChart.vue` 中：

```javascript
import { ref, onMounted, watch, computed, nextTick } from 'vue'
import Chart from 'chart.js/auto'
import { getEfficiencyTrend } from '@/api/stats' // 新增API导入

// 将硬编码数据改为响应式数据
const monthlyData = ref([])
const weeklyData = ref([])

// 获取真实数据
const fetchEfficiencyData = async () => {
  try {
    const teacherId = localStorage.getItem('userId')
    const res = await getEfficiencyTrend(teacherId, viewMode.value)
    
    if (res && res.data) {
      if (viewMode.value === 'month') {
        monthlyData.value = res.data.monthly
      } else {
        weeklyData.value = res.data.weekly
      }
      
      // 重新创建图表
      createMainChart()
      if (viewMode.value === 'month') {
        createComparisonChart()
      }
    }
  } catch (error) {
    console.error('获取教学效率趋势失败:', error)
    // 失败时使用默认数据
    useDefaultData()
  }
}

// 组件挂载时获取数据
onMounted(() => {
  fetchEfficiencyData()
})

// 监听视图模式变化时重新获取数据
watch(viewMode, () => {
  fetchEfficiencyData()
})
```

### 步骤 4：创建前端API文件

创建 `src/api/stats.js`（如果不存在）：

```javascript
import request from '@/utils/request'

// 获取教学效率趋势
export function getEfficiencyTrend(teacherId, type = 'month') {
  return request({
    url: `/api/stats/teacher/${teacherId}/efficiency-trend`,
    method: 'get',
    params: { type }
  })
}

// 获取教师统计信息
export function getTeacherStats(teacherId) {
  return request({
    url: `/api/stats/teacher/${teacherId}`,
    method: 'get'
  })
}

// 获取课程统计信息
export function getCourseStats(courseId) {
  return request({
    url: `/api/stats/course/${courseId}`,
    method: 'get'
  })
}
```

## 📊 数据计算示例

### 真实场景计算示例

假设教师ID为2的张教授：

#### 输入数据（从数据库查询）：
- 6月份平均成绩：85.5分
- 6月份课程完成率：88%
- 6月份学生参与度：82%
- 6月份授课课程数：24节
- 6月份活跃学生数：38人

#### 计算过程：
```javascript
// 教学效率 = 平均成绩权重 * 成绩百分比 + 完成率权重 * 完成率 + 参与度权重 * 参与度
efficiency = (85.5 * 0.4) + (88 * 0.3) + (82 * 0.3)
          = 34.2 + 26.4 + 24.6
          = 85.2%

// 但为了让数据更合理，可以进行归一化处理
normalizedEfficiency = (efficiency / 100) * 70 + 30  // 映射到30-100区间
                     = (85.2 / 100) * 70 + 30
                     = 59.64 + 30
                     = 89.64%
```

#### 输出结果：
```javascript
{
  month: '6月',
  efficiency: 57.33,  // 当前是模拟值，应该是 89.64
  courseCount: 24,
  studentEngagement: 75  // 当前是模拟值，应该是 82
}
```

## 🎯 总结

### 当前状态
- ❌ 使用硬编码的模拟数据
- ❌ 数据不会随时间变化
- ❌ 不反映真实教学情况

### 改进后状态
- ✅ 从数据库实时查询
- ✅ 基于真实教学数据计算
- ✅ 可追踪历史趋势
- ✅ 支持多维度分析

### 实施建议

1. **短期方案**（1-2天）：
   - 使用现有数据库表计算实时数据
   - 不需要新建表，直接查询现有数据

2. **长期方案**（1周）：
   - 创建专门的趋势统计表
   - 实现定时任务每日计算
   - 支持历史数据回溯

3. **优先级**：
   - 🔴 高优先级：连接真实数据源
   - 🟡 中优先级：优化计算算法
   - 🟢 低优先级：添加更多维度

## 📞 需要帮助？

如果您需要我帮您实现真实数据连接，请告诉我：
1. 您希望使用哪种方案（短期/长期）
2. 是否需要我编写完整的后端代码
3. 是否需要创建数据库表和SQL脚本

我可以立即为您实现完整的真实数据对接！


