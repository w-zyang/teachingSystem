<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.experiment.mapper.QuestionOptionMapper">
    
    <resultMap id="QuestionOptionResultMap" type="com.experiment.pojo.QuestionOption">
        <id column="id" property="id"/>
        <result column="question_id" property="questionId"/>
        <result column="key" property="key"/>
        <result column="content" property="content"/>
        <result column="is_correct" property="isCorrect"/>
        <result column="sort_order" property="sortOrder"/>
    </resultMap>
    
    <insert id="insert" parameterType="com.experiment.pojo.QuestionOption" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO question_option (question_id, `key`, content, is_correct, sort_order)
        VALUES (#{questionId}, #{key}, #{content}, #{isCorrect}, #{sortOrder})
    </insert>
    
    <select id="selectById" resultMap="QuestionOptionResultMap">
        SELECT * FROM question_option WHERE id = #{id}
    </select>
    
    <select id="selectByQuestionId" resultMap="QuestionOptionResultMap">
        SELECT * FROM question_option WHERE question_id = #{questionId} ORDER BY sort_order ASC
    </select>
    
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO question_option (question_id, `key`, content, is_correct, sort_order)
        VALUES
        <foreach collection="options" item="option" separator=",">
            (#{option.questionId}, #{option.key}, #{option.content}, #{option.isCorrect}, #{option.sortOrder})
        </foreach>
    </insert>
    
    <update id="update" parameterType="com.experiment.pojo.QuestionOption">
        UPDATE question_option 
        SET question_id = #{questionId}, `key` = #{key}, content = #{content}, 
            is_correct = #{isCorrect}, sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM question_option WHERE id = #{id}
    </delete>
    
    <delete id="deleteByQuestionId">
        DELETE FROM question_option WHERE question_id = #{questionId}
    </delete>
    
</mapper> 