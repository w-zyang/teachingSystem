<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.experiment.mapper.ChapterMapper">

    <!-- 根据ID查询章节 -->
    <select id="findById" resultType="com.experiment.pojo.Chapter">
        SELECT * FROM chapter WHERE id = #{id}
    </select>

    <!-- 根据课程ID查询章节列表 -->
    <select id="findByCourseId" resultType="com.experiment.pojo.Chapter">
        SELECT * FROM chapter WHERE course_id = #{courseId} ORDER BY sort_order ASC
    </select>

    <!-- 插入新章节 -->
    <insert id="insert" parameterType="com.experiment.pojo.Chapter" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chapter (
            course_id, title, description, duration, objectives, sort_order, create_time, update_time
        ) VALUES (
            #{courseId}, #{title}, #{description}, #{duration}, #{objectives}, #{sortOrder}, #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 更新章节信息 -->
    <update id="update" parameterType="com.experiment.pojo.Chapter">
        UPDATE chapter SET
            course_id = #{courseId},
            title = #{title},
            description = #{description},
            duration = #{duration},
            objectives = #{objectives},
            sort_order = #{sortOrder},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 删除章节 -->
    <delete id="deleteById">
        DELETE FROM chapter WHERE id = #{id}
    </delete>

    <!-- 根据课程ID删除章节 -->
    <delete id="deleteByCourseId">
        DELETE FROM chapter WHERE course_id = #{courseId}
    </delete>

    <!-- 更新排序 -->
    <update id="updateSortOrder">
        UPDATE chapter SET sort_order = #{sortOrder} WHERE id = #{id}
    </update>

</mapper> 