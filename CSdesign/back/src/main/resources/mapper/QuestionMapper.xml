<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.experiment.mapper.QuestionMapper">
    
    <resultMap id="QuestionResultMap" type="com.experiment.pojo.Question">
        <id column="id" property="id"/>
        <result column="exam_id" property="examId"/>
        <result column="type" property="type"/>
        <result column="content" property="content"/>
        <result column="answer" property="answer"/>
        <result column="analysis" property="analysis"/>
        <result column="score" property="score"/>
        <result column="difficulty" property="difficulty"/>
        <result column="knowledge_point" property="knowledgePoint"/>
        <result column="sort_order" property="sortOrder"/>
    </resultMap>
    
    <insert id="insert" parameterType="com.experiment.pojo.Question" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO question (exam_id, type, content, answer, analysis, score, difficulty, knowledge_point, sort_order)
        VALUES (#{examId}, #{type}, #{content}, #{answer}, #{analysis}, #{score}, #{difficulty}, #{knowledgePoint}, #{sortOrder})
    </insert>
    
    <select id="selectById" resultMap="QuestionResultMap">
        SELECT * FROM question WHERE id = #{id}
    </select>
    
    <select id="selectByExamId" resultMap="QuestionResultMap">
        SELECT * FROM question WHERE exam_id = #{examId} ORDER BY sort_order ASC
    </select>
    
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO question (exam_id, type, content, answer, analysis, score, difficulty, knowledge_point, sort_order)
        VALUES
        <foreach collection="questions" item="question" separator=",">
            (#{question.examId}, #{question.type}, #{question.content}, #{question.answer}, 
             #{question.analysis}, #{question.score}, #{question.difficulty}, #{question.knowledgePoint}, #{question.sortOrder})
        </foreach>
    </insert>
    
    <update id="update" parameterType="com.experiment.pojo.Question">
        UPDATE question 
        SET exam_id = #{examId}, type = #{type}, content = #{content}, answer = #{answer},
            analysis = #{analysis}, score = #{score}, difficulty = #{difficulty},
            knowledge_point = #{knowledgePoint}, sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM question WHERE id = #{id}
    </delete>
    
    <delete id="deleteByExamId">
        DELETE FROM question WHERE exam_id = #{examId}
    </delete>
    
</mapper> 