<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.experiment.mapper.LessonMapper">

    <!-- 根据ID查询课时 -->
    <select id="findById" resultType="com.experiment.pojo.Lesson">
        SELECT * FROM lesson WHERE id = #{id}
    </select>

    <!-- 根据章节ID查询课时列表 -->
    <select id="findByChapterId" resultType="com.experiment.pojo.Lesson">
        SELECT * FROM lesson WHERE chapter_id = #{chapterId} ORDER BY sort_order ASC
    </select>

    <!-- 插入新课时 -->
    <insert id="insert" parameterType="com.experiment.pojo.Lesson" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO lesson (
            chapter_id, title, content, video_url, resources, duration, sort_order, status, create_time, update_time
        ) VALUES (
            #{chapterId}, #{title}, #{content}, #{videoUrl}, #{resources}, #{duration}, #{sortOrder}, #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 更新课时信息 -->
    <update id="update" parameterType="com.experiment.pojo.Lesson">
        UPDATE lesson SET
            chapter_id = #{chapterId},
            title = #{title},
            content = #{content},
            video_url = #{videoUrl},
            resources = #{resources},
            duration = #{duration},
            sort_order = #{sortOrder},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 删除课时 -->
    <delete id="deleteById">
        DELETE FROM lesson WHERE id = #{id}
    </delete>

    <!-- 根据章节ID删除课时 -->
    <delete id="deleteByChapterId">
        DELETE FROM lesson WHERE chapter_id = #{chapterId}
    </delete>

    <!-- 更新排序 -->
    <update id="updateSortOrder">
        UPDATE lesson SET sort_order = #{sortOrder} WHERE id = #{id}
    </update>

</mapper> 